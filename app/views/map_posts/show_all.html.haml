%br
%br
%br
.container
	- @map_posts.each do |post|
		%span= post.title
		%span= post.video_id
		= link_to "Deletar", video_map_post_path(post, id: post.video_id), method: :delete, data: { confirm: "Tem certeza?" }, class: "btn btn-danger"